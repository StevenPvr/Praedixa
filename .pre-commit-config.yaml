default_install_hook_types:
  - pre-commit
  - pre-push
  - commit-msg
default_stages:
  - pre-commit

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: end-of-file-fixer
        stages: [pre-commit]
      - id: trailing-whitespace
        stages: [pre-commit]
      - id: check-merge-conflict
        stages: [pre-commit]
      - id: check-yaml
        exclude: "pnpm-lock.yaml"
        stages: [pre-commit]
      - id: detect-private-key
        stages: [pre-commit]
      - id: check-added-large-files
        args: ["--maxkb=500"]
        exclude: "^app-landing/public/dashboard-preview(-(?:1280|1920))?\\.(mp4|webm)$|^app-landing/public/dashboard-poster\\.jpg$|^app-landing/public/hero-video/hero-[a-z0-9-]+\\.(mp4|webm|jpg)$"
        stages: [pre-commit]

  - repo: local
    hooks:
      - id: prettier-staged
        name: prettier staged files
        entry: sh -c 'pnpm exec prettier --write "$@"' --
        language: system
        files: "\\.(ts|tsx|js|jsx|json|md|ya?ml)$"
        stages: [pre-commit]

      - id: gate-precommit-blocking
        name: gate pre-commit blocking (security + tests)
        entry: sh -c './scripts/gates/gate-precommit-blocking.sh'
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-commit]

      - id: gate-typecheck-all
        name: gate pre-push typecheck all
        entry: sh -c './scripts/gates/gate-typecheck-all.sh'
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-push]

      - id: gate-prepush-deep
        name: gate pre-push deep security (blocking)
        entry: sh -c './scripts/gates/gate-prepush-deep.sh'
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-push]

      - id: gate-report-verify-pre-push
        name: gate report verify (signed + fresh + HEAD-bound)
        entry: sh -c './scripts/gates/verify-gate-report.sh --mode manual --run-if-missing --max-age-seconds 21600'
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-push]

      - id: conventional-commit-message
        name: conventional commit message
        entry: sh -c './scripts/gates/check-commit-message.sh "$1"' --
        language: system
        stages: [commit-msg]
