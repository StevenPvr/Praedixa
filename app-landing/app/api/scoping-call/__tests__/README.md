# `app/api/scoping-call/__tests__/`

Tests de securite et de contrat HTTP pour la route `POST /api/scoping-call`.

## Objectif

- verifier les gardes de taille, origine et format ;
- garantir les headers sensibles, notamment `Cache-Control: no-store`.
