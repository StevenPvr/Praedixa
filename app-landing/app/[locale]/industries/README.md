# `app/[locale]/industries/`

Industry pages in English.

## Role

- host the English vertical pages under `/en/industries/[slug]` ;
- mirror the French sector pages while keeping localized slugs and metadata ;
- reuse the shared rendering logic from `components/pages/SectorPage.tsx`.
