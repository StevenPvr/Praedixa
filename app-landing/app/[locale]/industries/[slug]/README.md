# `app/[locale]/industries/[slug]/`

Dynamic EN route for industry pages.

## Role

- resolve industry content from `lib/content/sector-pages.ts` ;
- generate EN metadata plus FR/EN alternates ;
- render the shared page shell via `components/pages/SectorPage.tsx`.
