/**
 * Re-export Card from @praedixa/ui — single source of truth.
 * All variants (default, elevated, glass, interactive, premium, flat) are in packages/ui.
 */
export {
  Card,
  CardHeader,
  CardFooter,
  CardTitle,
  CardDescription,
  CardContent,
} from "@praedixa/ui";
