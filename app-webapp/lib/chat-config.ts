/** Polling interval for conversation/message refresh (ms). */
export const CHAT_POLL_INTERVAL_MS = 30_000;

/** Polling interval for live operational/forecast data (ms). */
export const LIVE_DATA_POLL_INTERVAL_MS = 10_000;
